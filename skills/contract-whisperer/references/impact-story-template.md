# Contract Impact Report: [API / Service Name]

> **Analyzed:** YYYY-MM-DD | **Schema type:** [OpenAPI / GraphQL / Protobuf / AsyncAPI / JSON]
> **Old version:** [tag / commit / filename] | **New version:** [tag / commit / filename]
> **Deployment safe as-is:** ✅ Yes / ⚠️ With caveats / ❌ No

---

## Executive Summary

_2–4 sentences. How many breaking changes were found? What is the highest-severity
impact? Is this safe to deploy without consumer coordination?_

**Change breakdown:**
| Category | Count |
|----------|-------|
| 🔴 Hard-breaking changes | N |
| 🟠 Silently-breaking changes | N |
| 🟡 Soft / degraded-only changes | N |
| 🟢 Non-breaking (additive) | N |
| ⚠️ Additive with hidden risks | N |

---

## Impact Stories

_One story per significant change or tightly-related change cluster. Ordered by severity._

---

### Story 1: [Plain-English Title — e.g., "Order total field removed from payment response"]

**Severity:** 🔴 Hard break / 🟠 Silent break / 🟡 Soft break

**The change:**
_What specifically changed in the contract. Be concrete: field name, old value/type, new value/type._

**The breach:**
_Which implicit or explicit contract was broken. Was this field documented? Guaranteed?
Part of a published spec version?_

**Consumer narratives:**

> **[Consumer name / type — e.g., iOS app, partner webhook, checkout service]**
> _A concrete, plain-English description of what this consumer will experience. Not
> "null pointer exception" — "the checkout screen will display $0.00 for the order total
> because `amount_due` is missing from the response. No error will be thrown; the amount
> will simply render as zero. This affects every order confirmation screen for all users."_

> **[Next consumer]**
> _Their specific experience._

**Coordination required:**
- [ ] [Specific action — e.g., "Notify iOS team: requires app update before server deploys"]
- [ ] [Specific action — e.g., "Add `amount_due` back as deprecated alias during transition"]
- [ ] [Specific action — e.g., "Publish deprecation notice to partner developers"]

**Migration path:**
_The safest sequence for making this change without breaking consumers. E.g., "Add new field
alongside old field → notify consumers → deprecate old field → set removal date → remove."_

---

### Story 2: [Plain-English Title]

**Severity:**

**The change:**

**The breach:**

**Consumer narratives:**

> **[Consumer]**

**Coordination required:**
- [ ]

**Migration path:**

---

<!-- Repeat for each impact story -->

---

## Non-Breaking Changes (with Notes)

_Changes that are safe for existing consumers, with any caveats._

| Change | Classification | Caveat |
|--------|---------------|--------|
| Added `loyalty_tier` field to user response | Additive | Strict deserializers with `additionalProperties: false` will reject; generated clients need regeneration |
| New `PENDING_REVIEW` enum value in `status` | Additive | Consumers using exhaustive switch/match will hit unhandled case |
| New `/v2/search` endpoint | Additive | None |

---

## Overall Deployment Assessment

**Safe to deploy as-is:** ✅ Yes / ⚠️ With caveats / ❌ No — requires consumer coordination

**Recommended approach:** [Version alongside / Deprecation window / Coordinated release / etc.]
_(See `references/versioning-strategies.md` for detailed options.)_

**Required notifications:**
- [ ] [Team / partner / consumer that must be notified before deployment]
- [ ] [Any SLA or contractual notice period to observe]

**Suggested migration timeline:**
_If changes require a migration window, what does a safe sequence look like?_

1. [Step 1 — e.g., "Deploy new version alongside old, old still active"]
2. [Step 2 — e.g., "Notify all consumers with 30-day migration window"]
3. [Step 3 — e.g., "Monitor consumer adoption via access logs"]
4. [Step 4 — e.g., "Sunset old version after window closes"]

---

## Schema Drift Log

_All individual field-level changes, for completeness and audit trail._

| Path | Change Type | Old | New | Breaking? |
|------|------------|-----|-----|-----------|
| `response.amount_due` | Removed | `number` | — | 🔴 Yes |
| `response.user.name` | Renamed | `name` | `full_name` | 🔴 Yes |
| `response.status` | Enum added | — | `PENDING_REVIEW` | ⚠️ Hidden risk |
| `response.loyalty_tier` | Added | — | `string \| null` | 🟢 No |

---

*Generated by Contract Whisperer skill | YYYY-MM-DD*