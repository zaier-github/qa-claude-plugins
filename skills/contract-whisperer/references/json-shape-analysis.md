# Plain JSON / TypeScript Shape Analysis

See `references/graphql-analysis.md` — the "Plain JSON / TypeScript Shape Analysis"
section covers TypeScript interfaces, Zod schemas, and raw JSON shapes.

Apply the same breaking change taxonomy as OpenAPI (field removed = breaking, type
changed = breaking, new optional field = non-breaking with caveats).